apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-internship-user-service
  labels:
    app: java-internship-user-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: java-internship-user-service
  template:
    metadata:
      labels:
        app: java-internship-user-service
    spec:
      containers:
        - name: java-internship-user-service
          image: maxononion/javainternship-user-service:latest
          ports:
            - containerPort: 8081
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: docker
            - name: SERVER_PORT
              value: "8081"
          envFrom:
            - secretRef:
                name: java-internship-user-service-secret
          startupProbe:
            tcpSocket:
              port: 8081
            failureThreshold: 36
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            tcpSocket:
              port: 8081
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 5
          livenessProbe:
            tcpSocket:
              port: 8081
            periodSeconds: 45
            timeoutSeconds: 5
            failureThreshold: 4
